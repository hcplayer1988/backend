"""App configuration for the termine app."""
 
from django.apps import AppConfig
 
 
class TermineConfig(AppConfig):
    """Configuration for the termine app."""
 
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'termine'
 